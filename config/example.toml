# NOTE: This specific file is a reference template and does not affect live project execution.
# It serves as a blueprint to show how your pipeline workflow configurations must be structured.
# 
# To create an active pipeline, copy this structure into your project root at:
# .runner/workflows/your_ci.toml

title = "Production Python Test Suite"
version = "1.0.0"
image = "python:3.12-slim"
shell = true

[jobs.install_reqs]
command = "pip install -r requirements.txt"
needs = []

[jobs.run_pytest]
command = "pytest tests/"
needs = ["install_reqs"]